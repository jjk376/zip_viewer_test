<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.sj.mapper.FileMapper">

	<select id="getTime" resultType="string">
	select now()
	</select>
	
	<insert id="insertFile">
	insert into zip_viewer.file (file_id, user_id, file_name, file_size, file_type, file_register_time) values
	(#{file_id}, #{user_id}, #{file_name}, #{file_size}, #{file_type}, #{file_register_time})
	</insert>

</mapper>